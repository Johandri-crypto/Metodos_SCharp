using System;

class Program
{
    static void EsParOImpar(int numero)
    {
        if (numero % 2 == 0)
            Console.WriteLine(numero + " es par.");
        else
            Console.WriteLine(numero + " es impar.");
    }

    static void Main()
    {
        EsParOImpar(7);
    }
}
