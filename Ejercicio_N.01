using System;

class Program
{
    static int Sumar(int a, int b)
    {
        return a + b;
    }

    static void Main()
    {
        int resultado = Sumar(5, 3);
        Console.WriteLine("La suma es: " + resultado);
    }
}
